This is a simple Python Script to test the functionality of a telegram bot.

Prepared for Docker:

- Add `.env` file for `BOT_TOKEN`
- Start: `docker-compose up --build`
- Start as deamon `docker-compose up -d`

